services:
  research-agent:
    build: .
    env_file:
      - .env
    volumes:
      - ./reports:/app/reports
    network_mode: "host"
    ports:
      - "8501:8501"
    # Run Dashboard (default): docker compose up
    # Run CLI: docker compose run --rm research-agent "Topic" python src/main.py "Topic"
